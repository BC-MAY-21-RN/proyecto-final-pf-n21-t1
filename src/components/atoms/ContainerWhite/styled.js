import styled from 'styled-components';
import {Color} from '../../../theme/default';
import {Box} from '../Container/styled';

export const BoxW = styled(Box)`
  background-color: ${Color.background};
  width: 100%;
`;
